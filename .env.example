# Frontend (Vercel) — public keys only; bundled into the client build
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your_publishable_or_anon_key

# Do NOT add OpenAI, Resend, service_role, or Google secrets here.
# Server secrets live in Supabase Dashboard → Edge Functions → Secrets:
#   OPENAI_API_KEY, RESEND_API_KEY, RESEND_FROM, SEND_EMAIL_HOOK_SECRET, etc.
